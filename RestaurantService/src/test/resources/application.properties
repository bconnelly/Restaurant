host.url=${FULLSTACK_HOST}
customer.get-all.url=${CUSTOMER_GET_ALL}
customer.get-by-name.url=${CUSTOMER_GET_BY_NAME}
customer.exists.url=${CUSTOMER_EXISTS}
customer.seat.url=${CUSTOMER_SEAT}
order.submit.url=${ORDER_SUBMIT}
table.get-all.url=${TABLE_GET_ALL}